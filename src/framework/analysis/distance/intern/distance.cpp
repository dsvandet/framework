#include "distance.hpp"

int distance_one(int a, int b) {
    std::cout << "cpp: distance_one:" << a+b+1 << std::endl;
    return a+b+1;
}

int distance_two(int a, int b) {
    std::cout << "cpp: distance_two:" << a+b+2 << std::endl;
    return a+b+2;
}